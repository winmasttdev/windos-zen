# windOS Zen — systemd-zram-generator configuration
# Provides compressed RAM swap by default (zstd). Tuned for legacy/low-spec
# hardware: up to 2x RAM, high priority so it is preferred over disks.
[zram0]
zram-size = ram * 2
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
